export type ScrollbarProps = {
  // 容器高度
  height?: string | string;
  // 是否显示原生滚动条
  native?: boolean;
  // 是否总是显示滚动条
  always?: boolean;
  // 是否横向显示
  horizontal?: boolean;
  // 滚动条宽度
  size?: number;
  // 与边间距
  space?: number;
  // 是否显示
  show?: boolean;
};
