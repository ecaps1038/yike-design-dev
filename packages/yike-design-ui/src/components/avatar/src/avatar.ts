
export type AvatarProps = {
  shape?: 'circle' | 'square';
  size?: number | string;
  icon?: string;
  imgUrl: string;
}

export type AvatarGroupProps = {
  shape?: 'circle' | 'square';
  size?: number | string;
  max?: number;
}
